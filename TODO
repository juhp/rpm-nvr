- support RPM epochs (ENVR/EVR)
  - maybe Package could include Maybe Epoch


class Versioned a where
  takeVersion :: a -> String

instance Versioned NV where
  takeVersion (NV _ v) = v

instance Versioned NVR where
  takeVersion (NVR
